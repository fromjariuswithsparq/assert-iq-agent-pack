# Rendered hook config (generated by install.sh / install.ps1 with absolute pack root).
# The committed source is hooks/hooks.template.json.
hooks/hooks.json

# Generated Claude artifacts (created by install.sh / install.ps1 / scripts/bootstrap.*).
# .claude/settings.json is rendered from hooks/hooks.template.json with absolute pack root.
# .claude/skills is a symlink to ../.github/skills (or a copy on Windows without dev-mode).
.claude/settings.json
.claude/skills

# Bootstrap install manifest (per-workspace, contains absolute paths).
.assert-iq/.install-manifest.json

# OS / editor noise
.DS_Store
**/.DS_Store
Thumbs.db
